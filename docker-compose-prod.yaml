services:
  flask-gunicorn-app:
    image: servingmlmodels-flask-gunicorn-app:1.0.0
    restart: always

  nginx:
    image: servingmlmodels-nginx:1.0.0
    ports:
      - "80:80"
      - "443:443"
    restart: always
